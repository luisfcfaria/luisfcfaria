spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: update
      show-sql: true
  datasource:
    url: jdbc:mariadb://localhost:3306/HOMEBREW
    username: root
    password: mypass